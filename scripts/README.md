# Repository scripts

Small maintenance utilities that are not part of the Nexus runtime live here.

Run the translation helper from the repository root with:

```bash
python scripts/translate_to_english.py
```
