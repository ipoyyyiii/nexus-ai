# workflow
- Prefers using Command Code slash commands such as `/plans` to access and manage reusable implementation plans. Confidence: 0.88
- Before implementing substantial changes, prepare a concrete implementation plan for the user to review and approve; do not modify code while the work is still in plan-review mode. Once the plan is explicitly approved, begin implementation directly in YOLO mode when requested, prioritizing the main workflow gaps before secondary hardening. Confidence: 0.97
- Prioritize making the AI agent feel like a polished, highly adaptive Claude/GPT-style conversational assistant before expanding pentesting/scanning capabilities. Focus first on natural multi-turn chat, streaming, durable context/memory, robust model fallback, and conversational tool proposals. Confidence: 0.96
- Before making code changes, read and understand all existing code and file relationships in the repository first — including exhaustive deep-read of all tools/*.py, frontend, and core modules — and transparently disclose what was read deeply vs sampled when asked. Confidence: 0.85
- Before making assessments or judgments about code quality/completeness, read all relevant code files thoroughly first — do not evaluate based on a subset — and explicitly disclose coverage gaps (sampled vs line-by-line) when asked about completeness. Confidence: 0.88
- Execute vulnerability scanning agents in sequential phases (recon → analysis → exploitation → assessment) with user pause/continue between each phase. Confidence: 0.70
- After initial assessment/audit, implement all required changes comprehensively in one shot — do not ask for piecemeal confirmation between sub-tasks. Confidence: 0.65
- When completing any change, immediately apply it to Docker (Dockerfile + docker-compose) and verify the full stack runs end-to-end — changes are not "done" until they work in the container. Confidence: 0.85
- Install and integrate all relevant external tools into the Dockerfile in a single session; do not split tool installation across multiple sessions. Confidence: 0.80
- After any session of code changes, rebuild and run Docker to verify everything works before finishing. Confidence: 0.80
- For testing AI pipelines, prefers avoiding paid model usage and validating the failure path when credits are unavailable; paid models should not be consumed implicitly. Confidence: 0.88
- Frontend layouts must remain fully usable at the default 100% browser zoom, with configuration controls such as Stealth Mode visible without requiring users to zoom out. Confidence: 0.92
- Treat Supabase schema migrations as one-time SQL Editor setup steps, not actions repeated on each login or session; clearly state when a migration is complete and whether a restart/rebuild is actually required. Confidence: 0.92
- When handing work off to another coding/AI provider, create a reusable Markdown roadmap plus a copy-paste implementation prompt tailored to the repository, goals, and remaining context rather than relying on conversational context alone. Confidence: 0.93
- Prefers hosting the LLM locally via Kaggle/Colab using an OpenAI-compatible server (vLLM/Ollama/FastAPI wrapper) exposed through an ngrok tunnel, configuring Nexus via NEXUS_LLM_BASE_URL / NEXUS_LLM_API_KEY / NEXUS_CHAT_MODEL and using host.docker.internal for local Docker networking to avoid paid API credit consumption. Confidence: 0.90
- Prefers an additive LLM provider architecture where OpenRouter models and locally-hosted OpenAI-compatible models (Kaggle/Colab via ngrok) are exposed as parallel manual-select options in the registry/UI (tier `local`, id prefix `local-`) rather than automatic fallback routing — user explicitly wants to keep all existing OpenRouter models and add local models as extra choices to pick from directly when needed. Confidence: 0.95
- Prefers consolidating local LLM hosting to a single ngrok tunnel/base URL serving multiple models (dolphin, ravenxcyber) concurrently, with model availability driven by NEXUS_LOCAL_LLM_MODELS and automatic /v1 normalization, favoring a DRY single-URL .env config over duplicate per-model base URL variables and preferring complete removal of legacy redundant vars (e.g., DOLPHIN3_API_BASE) rather than retaining them for backward compatibility; expects manual .env update + `docker compose up -d --force-recreate` restart when ngrok regenerates an ephemeral URL. Confidence: 0.94
- Prefers to manually control Docker backend builds/restarts himself and wants the assistant to kill/cancel any long-running background builds on request rather than auto-building in background. Confidence: 0.85
- Prioritizes thoroughness/completeness over strict cost efficiency for AI agent phases — if all generated input/output and tool context is genuinely needed, considers higher token usage acceptable rather than requiring aggressive optimization; explicitly re-affirmed that broad recon (20+ tools) and ~60 vuln-type analysis breadth is intentional and acceptable. Confidence: 0.85
- Prefers to complete thorough codebase deep-read/grounding first before discussing next planning, then collaboratively decide prioritization together ('gimana enaknya') rather than having the assistant auto-pick the next focus area. Confidence: 0.82
- Requires implementation plans to include an explicit 'cara kerja' walkthrough explaining how dynamic/adaptive runtime behavior will operate (inputs, branching, failure-driven pivots) so he can evaluate/grade before approving execution. Confidence: 0.88
- Prefers residual hardcoded tuning knobs categorized as 'agak hardcoded' (FALLBACK_PRIORITY, max_pages/max_depth, TECH_TOOL_MAP/smart_selector mappings, stealth jitter) to be externalized to hybrid editable YAML (config/pentest_config.yaml) where code holds safe defaults and YAML only overrides when present (optional, deletion falls back to defaults without breaking), with defaults calibrated to sweet spots commonly seen in real-world world-class pentesting engagements (e.g., max_pages 40-80, max_depth 3, form > api_link > button > link weighting) — distinct from 'wajar' hardcoded payload libraries which should remain in code. Confidence: 0.88
- Prefers to audit and synchronize documentation/config artifacts (README, docker-compose.yaml, requirements, CHANGELOG, .gitignore) with current code state before pushing to GitHub, and when publishing publicly requires sanitizing hardcoded secrets/infrastructure details (API keys, VPS IPs) into env-var placeholders with .env.example while ensuring requirements/README/CHANGELOG accurately reflect current dependencies and features. Confidence: 0.88
- Prefers to handle git add/commit/push manually himself after the assistant patches files, rather than having the assistant auto-commit or auto-push to GitHub. Confidence: 0.87
- Expects clear Docker lifecycle guidance distinguishing code/Dockerfile changes (require `docker compose build` + `up -d --force-recreate`) from env/URL-only changes (require only `up -d --force-recreate` without rebuild), and treats bare `docker compose restart` as insufficient for loading new images. Confidence: 0.88
- Prefers scan configuration that combines two high-level presets (recon-only vs full) with retained granular vulnerability type selection (23 vuln types with Select All/Deselect All) — preset and vuln_types coexist, are end-to-end wired to backend (persisted as session_context.scan_preset/scan_vuln_types and used to scope vuln_task, disabled/ignored when preset is recon-only) rather than preset-only simplification. Confidence: 0.88
- Strongly dislikes placebo UI controls where frontend settings appear configurable but have no backend effect; expects every UI toggle/selector (ScanConfig vuln types, stealthMode/autoPilot, per-agent model selectors) to be end-to-end wired — e.g., ScanConfig filtering smart_selector tool selection, stealthMode controlling rate_limiter, autoPilot gating require_approval, and selected models actually routed via build_llm — and reacts negatively ("gak ngaruh anjir") when discovering they are disconnected. Confidence: 0.90
- Expects export functionality to be end-to-end working across three formats and wants scanning results evaluated from end-user/UX perspective (readability, usability, presentation) not just technical completeness — asks 'beneran bekerja gak' / 'udah oke belom dari segi user' as validation gate. Confidence: 0.78
- Prefers desktop-only frontend; explicitly deprioritizes mobile responsiveness as mobile is not target audience. Confidence: 0.90
- Defines 'backend sehat' as end-to-end browser-visible success (AI models load, reports appear) and rejects internal container-only validation (e.g., curl 200 inside docker to localhost:8000); expects diagnosis and fixes to cover frontend baked NEXT_PUBLIC_API_URL/public IP, CORS/proxy, and actual browser network reachability when UI fails — reacts with strong frustration ("bego ... gimana bisa backendnya lu bilang sehat cok") when health is claimed from internal checks while the browser UI fails. Confidence: 0.92